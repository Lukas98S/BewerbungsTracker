<UserControl x:Class="BewerbungsTracker.BewerberAnsicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BewerbungsTracker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="1" Margin="10" VerticalAlignment="Center">

            <TextBlock Text="Firma:" VerticalAlignment="Center" Margin="0,0,5,5"/>
            <TextBox x:Name="TxtFirma" Width="120" Margin="0,0,15,5" Padding="2"/>

            <TextBlock Text="Position:" VerticalAlignment="Center" Margin="0,0,5,5"/>
            <TextBox x:Name="TxtPosition" Width="120" Margin="0,0,15,5" Padding="2"/>

            <Button Content="Manuell hinzufügen" Click="BtnManuell_Click" Padding="10,5" Margin="0,0,10,5"/>
            <Button Content="Alles Löschen" Click="BtnDelete_Click" Padding="10,5" Margin="0,0,10,5"/>
            <Button x:Name="BtnImport" Content="PDFs Import" Click="BtnImport_Click" Padding="10,5" Margin="0,0,0,5"/>

        </WrapPanel>

        <DataGrid x:Name="BewerbungsTabelle"
              AutoGenerateColumns="False"
              Margin="10"
              IsReadOnly="True"
              Grid.Row="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Firma" Binding="{Binding Firma}" Width="*"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="*"/>
                <DataGridTextColumn Header="Bewerbungsdatum"
                                Binding="{Binding Datum, StringFormat=\{0:dd.MM.yyyy\}}"
                                Width="Auto"/>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
